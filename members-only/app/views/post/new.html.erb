<h1>Create Posts</h1>

<%= form_for(@post, url: post_path ) do |f| %>
	<%= f.label :title %>
	<%= f.text_field :title %><br>

	<%= f.label :body %>
	<%= f.text_field :body %><br>

	<%= f.submit "Create post" %>
<% end %>

